# Legacy storefront path: keep old bookmarks working after the subdomain cutover.
location = /gb-widget {
    return 301 https://widget.garment-buro.ru/;
}

location ^~ /gb-widget/ {
    rewrite ^/gb-widget/(.*)$ https://widget.garment-buro.ru/$1 permanent;
}
